import XCTest
@testable import UI

// Visual components are verified by eye / previews; this placeholder only
// proves the test target builds.
final class UIThemeTests: XCTestCase {
    func testModuleCompiles() {
        _ = AppTheme.self
    }
}
