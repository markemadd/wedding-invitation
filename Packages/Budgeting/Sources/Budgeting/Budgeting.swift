// Budgeting module — filled in during Phase 3.
//
// Will contain: SalaryAllocationService (single-income percentage allocation
// with the exactly-100% validation rule), the monthly grouped-SQL summary,
// and category bucket logic (fixed / nonMonthlyRecurring / flexible) that
// Phase 4's goal-planner cut suggestions depend on.

/// Namespace marker so the module compiles before Phase 3 adds real code.
public enum BudgetingModule {}
